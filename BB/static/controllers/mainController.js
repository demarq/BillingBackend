billing.controller('mainController', function MainController($scope,
                                                               $mdSidenav,
                                                               $location,
                                                               ){

});
